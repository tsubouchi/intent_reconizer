{
  "name": "hackathon0928",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "pnpm --dir apps/frontend dev",
    "dev:frontend": "pnpm --dir apps/frontend dev",
    "dev:router": "pnpm --dir apps/intent-router dev",
    "dev:manifest": "pnpm --dir apps/manifest-generator dev",
    "lint": "pnpm -r --if-present lint",
    "test:unit": "pnpm -r --if-present test:unit",
    "type-check": "pnpm -r --if-present type-check",
    "contract": "node ./tools/contracts/run-contract.mjs"
  }
}
